{
    "name": "jo-basket-ball",
    "version": "1.3.0",
    "type": "module",
    "scripts": {
        "build": "tsc && cp -r src/translations dist/",
        "dev": "tsc --watch",
        "all": "html-validator --quiet"
    },
    "devDependencies": {
        "w3c-html-validator": "^2.2.0"
    }
}