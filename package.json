{
    "name": "jo-basket-ball",
    "version": "1.3.0",
    "type": "module",
    "scripts": {
        "build": "tsc && cp -r src/translations dist/",
        "dev": "tsc --watch",
        "serve": "npx serve .",
        "validate": "html-validate 'articles/**/*.html' '*.html'"
    },
    "devDependencies": {
        "html-validate": "^10.11.2"
    }
}
